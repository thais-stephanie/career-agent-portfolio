"""ATS adapters.

Every module here translates one vendor's representation into the
provider-neutral objects in `base.py`. Nothing here interprets meaning: no
career intent, no eligibility, no geography decisions, no software matching, no
ranking, no relevance scoring. A test enforces that by import inspection.
"""
